#!/usr/bin/python3

numbers = {2, 4, 4, 6}

iterator = iter(numbers)

print(f"numbers element: {next(iterator)}")
print(f"numbers element: {next(iterator)}")
print(f"numbers element: {next(iterator)}\n")
