# Copyright (C) 2019-2026 Katarzyna Krasińska
# Languagium - https://github.com/katheroine/languagium
# Licensed under GPL-3.0 - see LICENSE.md
# SPDX-License-Identifier: GPL-3.0-or-later

f = float(0.0)
print(f"f = float(0.0) # {f} ({type(f)})")

f = float(5.2)
print(f"f = float(5.2) # {f} ({type(f)})")

f = float(-5.2)
print(f"f = float(-5.2) # {f} ({type(f)})")

f = float(0)
print(f"f = float(0) # {f} ({type(f)})")

f = float(5)
print(f"f = float(5) # {f} ({type(f)})")

f = float(-5)
print(f"f = float(-5) # {f} ({type(f)})")

f = float("0.0")
print(f"f = float(\"0.0\") # {f} ({type(f)})")

f = float("5.2")
print(f"f = float(\"5.2\") # {f} ({type(f)})")

f = float("-5.2")
print(f"f = float(\"-5.2\") # {f} ({type(f)})")

f = float("0")
print(f"f = float(\"0\") # {f} ({type(f)})")

f = float("5")
print(f"f = float(\"5\") # {f} ({type(f)})")

f = float("-5")
print(f"f = float(\"-5\") # {f} ({type(f)})")
