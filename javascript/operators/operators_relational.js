#!/usr/bin/env node

let a = 1, b = 2, c = false;

c = a < b
console.log(`${a} < ${b} = ${c}`);
c = a > b
console.log(`${a} > ${b} = ${c}`);
c = a <= b
console.log(`${a} <= ${b} = ${c}`);
c = a >= b
console.log(`${a} >= ${b} = ${c}`);
c = a == b
console.log(`${a} == ${b} = ${c}`);
c = a != b
console.log(`${a} != ${b} = ${c}`);
