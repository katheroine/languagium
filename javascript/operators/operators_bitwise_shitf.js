#!/usr/bin/env node

let n = 4; // 00100

console.log(`${n} >> 1 = ${n >> 1}`); // 00010 // 2
console.log(`${n} >> 2 = ${n >> 2}`); // 00001 // 1
console.log(`${n} << 1 = ${n << 1}`); // 01000 // 8
console.log(`${n} << 2 = ${n << 2}`); // 10000 // 16
