#!/usr/bin/env node

let sententia = "   Sapere aude.   ";
console.log(`sententia = <${sententia}>\n`);

let trimmed_sententia = sententia.trim();

console.log(`trimmed sententia = <${trimmed_sententia}>\n`);
